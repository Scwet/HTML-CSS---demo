HTML是超文本标记语言，用来定义网页结构和内容
HTML由元素组成，通过在文本前面加上开头标签，后面加上一个结尾标签，可以把文本变成元素
eg:
<开头标签>文本</结尾标签>

标签不区分大小写，但推荐小写
标签分双标签和单标签
双标签:（有开头标签和结尾标签）
<marquee>内容</marquee> 内容从右向左滚动出现，无限循环

单标签:（只有开头标签）
<input> 出现一个可以输入的文本框

标签属性是给标签提供的附加信息
标签属性由“属性名”和“属性值”组成（但是存在特殊情况，只存在属性名就行），一个标签可以有多个标签属性，但是标签名不能重复（重复的话只有第一个有效）
eg:
<marquee loop="1">
loop是标签名，1是标签值，让循环只执行一次
标签分为全局属性和特有属性。全局属性：任何标签都能用，特有属性：特定标签专用。

HTML最基本的结构:

<!doctype html>    声明文档类型为 HTML5
<html>
  <head>
    <title>标题</title>    浏览器标签页标题
  </head>
  <body>
    内容    用户可见的内容区域
  </body>
</html>
